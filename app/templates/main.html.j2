{% extends "base.html.j2" %}

{% block content %}
  <section class="container">
    <div class="row">
      <div class="col">
        <a class="btn" href="{{ url_for('login') }}">
          Login
        </a>
        <a class="btn" href="{{ url_for('registration') }}">
          Registration
        </a>
      </div>
    </div>
  </section>
{% endblock %}
